# ==============================================
# CONFIGURACIÓN DEL ENTORNO - VARIABLES REQUERIDAS
# ==============================================
# Copia este archivo como .env y completa los valores

# --- SEGURIDAD ---
# Clave secreta de Django (genera una segura para producción)
SECRET_KEY=tu-clave-secreta-aqui

# --- BASE DE DATOS ---
# URL de conexión PostgreSQL (Railway la proporciona automáticamente)
DATABASE_URL=postgres://usuario:password@host:puerto/nombre_db

# --- GO HIGH LEVEL (OAuth2 Marketplace) ---
# Credenciales de tu App en GHL Marketplace
GHL_CLIENT_ID=tu-client-id-de-ghl
GHL_CLIENT_SECRET=tu-client-secret-de-ghl

# URL de callback (debe coincidir con la del Marketplace)
GHL_REDIRECT_URI=https://tu-dominio.railway.app/api/oauth/callback/
# GHL_REDIRECT_URI = os.environ.get('GHL_REDIRECT_URI', 'http://localhost:8000/api/oauth/callback/')

# --- DOMINIOS PERMITIDOS (Producción) ---
# Lista separada por comas de hosts permitidos
ALLOWED_HOSTS=tu-dominio.railway.app,*.up.railway.app

# --- VERIFICACION DE WEBHOOKS ---
# Secreto compartido con GHL para verificar la firma HMAC de los webhooks
# Si no se configura, los webhooks se aceptan sin verificacion (solo para desarrollo)
GHL_WEBHOOK_SECRET=tu-webhook-secret-aqui

# --- CORS (Origenes permitidos para peticiones cross-origin) ---
# Lista de dominios permitidos para CORS (separados por coma)
CORS_ALLOWED_ORIGINS=https://app.gohighlevel.com,https://widgets.leadconnectorhq.com
